# Legacy Outputs

This directory preserves historical scratch files and generated outputs from the
original script-based workflow. They are useful as reference material while the
package gains small shareable fixtures, but they are not part of the maintained
test suite.

New generated output should go under `build/` or another ignored work directory.
